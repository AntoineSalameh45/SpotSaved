import {StyleSheet} from 'react-native';

const albumGalleryStyles = StyleSheet.create({
  rowContainer: {
    flexDirection: 'row',
    borderBottomWidth: 2,
    borderBottomColor: '#fff',
  },
  viewContainer: {
    marginVertical: 10,
  },
});
export default albumGalleryStyles;
